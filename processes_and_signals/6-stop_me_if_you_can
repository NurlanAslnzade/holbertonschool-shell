#!/usr/bin/env bash
# This script stops the running 4-to_infinity_and_beyond process without using 'kill' or 'killall'.

# Həll yolu: `kill` sözünü istifadə etmədən prosesi dayandırmaq üçün `pkill` əmrindən istifadə olunur.
# -f flagı tam komanda sətrinə uyğun gəlməyi təmin edir.

pkill -f 4-to_infinity_and_beyond
