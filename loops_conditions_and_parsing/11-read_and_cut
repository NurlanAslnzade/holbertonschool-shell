#!/usr/bin/env bash
# This script reads the content of /etc/passwd line by line using a while loop 
# and displays only the username, user ID (UID), and home directory path.

# 'while IFS=: read -r F1 F2 F3 F4 F5 F6 F7' constructs the loop:
# 1. IFS=: sets the Internal Field Separator to the colon (:), 
#    so 'read' separates fields based on the delimiter used in /etc/passwd.
# 2. read -r F1 ... F7 reads each line of the input into 7 variables (F1 to F7).
# 3. < /etc/passwd redirects the file content as input to the while loop.

while IFS=: read -r F1 F2 F3 F4 F5 F6 F7; do
    # F1: Username
    # F3: User ID
    # F6: Home directory path
    
    # We print the required fields separated by a colon (:)
    echo "${F1}:${F3}:${F6}"
done < /etc/passwd
